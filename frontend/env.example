# This file is for environment variables used by the Vite development server.
# Vite automatically loads variables prefixed with VITE_

# While the current api.ts does not use this, it is good practice to have it.
# The backend URL is proxied by Nginx in the docker-compose setup.
VITE_API_BASE_URL=/api/v1 